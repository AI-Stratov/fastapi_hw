# Fastapi TODO

```bash
    cd src/backend
    cp .env.example .env
    cd ../..
    docker-compose up --build
```

Приложение будет доступно по адресу: http://localhost:5000

Миграции выполняются в dockerfile

docker-compose поднимает postgres и back

.env = .env.example в сценарии запуска через докер

Есть пользователи, но нет ролевки. Стратегия проверки прав по задачам - сокрытие (404),
если задача принадлежит другому пользователю.

Я постарался сделать формочки (Fastapi.Form), чтобы удобнее через Swagger проверять (удобнее чем json), но получилось не
супер.
Не сильно заморачивался с логикой валидации Task и User, может что-то упустил.

Кеширование сделано для топ задач (через cachetools.TTLCache)
